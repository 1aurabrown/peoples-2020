<section class="featured-collections half-grid">
  {% for block in section.blocks %}
    {% assign collection = collections[block.settings[coll]] %}
    <a class="y" href="{{ collection.url }}">
      <div class="grid-item rel x y max-full-height">
        <div class="abs f jcc aic z1 fill x y">
          <h2 class="ac h1 tc-white">{{ block.settings.text }}</h2>
        </div>
        <responsive-img-tag
          alt="{{ collection.title }}"
          class-name="x y"
          :src='{{ block.settings.image | json }}'>
        </responsive-img-tag>
      </div>
    </a>
  {% endfor %}
</section>

{% schema %}
  {
    "name": "Featured Collections",
    "class": "index-section",
    "max_blocks": 2,
    "blocks": [
      {
        "type": "collection",
        "name": "Collection",
        "settings": [
          {
            "label": "Collection",
            "id": "collection",
            "type": "collection"
          },
          {
            "type": "image_picker",
            "id": "image",
            "label": "Image"
          },
          {
            "type": "text",
            "id": "text",
            "label": "text"
          }
        ]
      }
    ],
    "presets": [
      {
        "name": "Featured Collections",
        "category": "Collection",
        "blocks": [
          {
            "type": "collection",
            "settings": {
              "text": "Shop Men"
            }
          },
          {
            "type": "collection",
            "settings": {
              "text": "Shop Women"
            }
          }
        ]
      }
    ]
  }
{% endschema %}